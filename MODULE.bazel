# This project manages ALL external dependencies through WORKSPACE
# (see .bazelrc `build --enable_workspace`). This file is intentionally
# empty: Bazel 7 requires it to exist when Bzlmod is enabled by default.
# Do NOT add dependencies here; add them in WORKSPACE / tools/workspace/.
