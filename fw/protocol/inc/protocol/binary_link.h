// Compatibility shim — definition lives in telemetry/binary_link.h (F12 jumps there).
#pragma once

#include "telemetry/binary_link.h"

namespace protocol
{
using BinaryLink = ::telemetry::BinaryLink;
}
